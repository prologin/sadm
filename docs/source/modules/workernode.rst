Match Workers
=============

The worker nodes are taking advantage of the RPC module:
each worker is a server the match dispatcher can call functions like:
- compile a champion
- run it and send back the match result
