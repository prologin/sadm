* [pm] pam_udb
  * mostly done

* pssh mdb wrappers
  * 30min task, can wait until we have the machines

* simple monitoring system (ping, users connected, matches launched)
  * integrated with mdb

* Syncing services
  * udb
    * authorized_keys for root
    * /etc/{passwd,shadow}
    * minecraft authdb
    * bug tracker
    * mdb/udb/home web admin + concours (django generic)

* [mareo] IRC server setup

* [seirl] docs for stechec2 and sadm to http://docs/

* [low priority]
  * Put common code from *db/*dbapi/receivers.py to a library
